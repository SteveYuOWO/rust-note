/**
 * - cargo new hello_cargo
 * - cargo build
 * - cargo build --relase
 * - cargo run
 * - cargo check
 */
fn main() {
    println!("Hello, world!");
}

// toml(tom's obvious, minimal language) is a language for describing configuration files.
// -- name, project name
// -- version, project version